% ============================================================
%  CHƯƠNG 4: THIẾT KẾ PHẦN MỀM
% ============================================================
\chapter{Thiết kế phần mềm}
\label{chap:software}

\section{Kiến trúc phần mềm tổng quan}
\label{sec:sw_architecture}

Phần mềm của hệ thống được thiết kế theo kiến trúc hướng sự kiện (Event-driven) trên nền tảng Dual-MCU. Sự tách biệt giữa tầng xử lý trí tuệ nhân tạo (ESP32-S3) và tầng điều phối ngoại vi (STM32F411CEU6) giúp tối ưu hóa tài nguyên phần cứng, đảm bảo tính ổn định và khả năng mở rộng của hệ thống:

\begin{itemize}
  \item \textbf{STM32 Firmware}: Đảm nhận vai trò "Bộ não điều phối". Firmware được viết bằng C thuần túy sử dụng thư viện HAL, hoạt động theo kiến trúc vòng lặp siêu vòng (\textit{superloop}) không chặn (non-blocking). Việc tích hợp DMA (Direct Memory Access) giúp xử lý luồng dữ liệu ngoại vi tốc độ cao mà không làm gián đoạn các tác vụ hiển thị giao diện hay điều khiển relay.
  \item \textbf{ESP32-S3 Firmware}: Đảm nhận vai trò "Bộ xử lý AI". Hoạt động trên nền tảng ESP-IDF kết hợp FreeRTOS, phân phối các tác vụ thu nhận ảnh và xử lý thuật toán nhận diện khuôn mặt trên các nhân xử lý độc lập để đạt tốc độ FPS tối đa.
\end{itemize}

\section{Firmware STM32F411CEU6: Bộ điều phối hướng sự kiện}
\label{sec:stm32_fw}

\subsection{Cấu trúc nhận dữ liệu UART DMA với Idle Line Detection}

Giao tiếp giữa STM32 và ESP32-S3 là xương sống của hệ thống. Thay vì sử dụng ngắt nhận từng byte truyền thống (Interrupt-driven) vốn gây tốn tài nguyên CPU và tiềm ẩn nguy cơ mất dữ liệu khi ESP32 gửi chuỗi thông điệp liên tiếp, firmware STM32 được thiết kế sử dụng \textbf{UART DMA kết hợp ngắt Idle Line}:

\begin{itemize}
  \item \textbf{Direct Memory Access (DMA)}: Dữ liệu từ ESP32 được bộ điều khiển DMA tự động chuyển thẳng vào bộ đệm trong RAM (\texttt{esp32\_dma\_rx\_buf}) mà không yêu cầu CPU can thiệp vào từng byte.
  \item \textbf{Idle Line Detection}: Ngắt chỉ được kích hoạt khi đường truyền UART rơi vào trạng thái rảnh (Idle), đánh dấu sự kết thúc của một gói tin hoàn chỉnh. CPU chỉ thức dậy để phân tích một dòng lệnh trọn vẹn.
  \item \textbf{Hàng đợi tin nhắn (Message Queue)}: Các gói tin sau khi nhận được đẩy vào một hàng đợi phần mềm (\texttt{uart1\_queue}). Cơ chế này đảm bảo STM32 không bỏ sót bất kỳ phản hồi nào từ ESP32 ngay cả khi đang bận xử lý giao diện OLED.
\end{itemize}

\subsection{Máy trạng thái hệ thống (FSM)}

Để đảm bảo tính thời gian thực và trải nghiệm mượt mà, hệ thống STM32 vận hành một máy trạng thái hữu hạn (FSM) mở rộng, được điều khiển hoàn toàn bằng sự kiện và bộ đếm thời gian hệ thống (\texttt{HAL\_GetTick()}), loại bỏ hoàn toàn các hàm chờ (\texttt{HAL\_Delay()}).

\begin{table}[H]
  \centering
  \caption{Các trạng thái của máy trạng thái hệ thống STM32}
  \label{tab:fsm_states}
  \begin{tabularx}{\textwidth}{l l X}
    \toprule
    \textbf{Trạng thái} & \textbf{Tên} & \textbf{Mô tả} \\
    \midrule
    \texttt{SYS\_CONNECTING} & Kết nối & Chờ tín hiệu \texttt{READY} từ ESP32 sau khi cấp nguồn. Nếu quá timeout, chuyển sang ngoại tuyến. \\
    \texttt{SYS\_IDLE}       & Chờ         & Trạng thái mặc định. OLED hiển thị số lượng khuôn mặt đã lưu. Hệ thống sẵn sàng nhận diện hoặc chờ lệnh. \\
    \texttt{SYS\_UNLOCKING}  & Mở khóa     & Relay kích hoạt, mở khóa solenoid. Sử dụng Timer không chặn để tự động đóng cửa sau \SI{3}{s}. \\
    \texttt{SYS\_ENROLLING}  & Đăng ký     & STM32 gửi lệnh \texttt{ENROLL} cho ESP32. DFPlayer phát âm thanh hướng dẫn đa góc độ. \\
    \texttt{SYS\_DELETING}   & Xóa dữ liệu & STM32 gửi lệnh \texttt{DEL\_ALL}, chờ phản hồi \texttt{DELETED} từ ESP32. \\
    \texttt{SYS\_DENIED}     & Từ chối     & OLED hiển thị ``Từ chối'', DFPlayer phát cảnh báo. Tự động về \texttt{SYS\_IDLE} sau \SI{2.5}{s}. \\
    \texttt{SYS\_LOCKED}     & Khóa an ninh & Khóa bảo vệ hệ thống trong \SI{5}{phút} khi truy cập sai nhiều lần. Hiển thị bộ đếm ngược. \\
    \texttt{SYS\_OFFLINE}    & Ngoại tuyến & Hiển thị cảnh báo lỗi giao tiếp khi mất kết nối UART hoặc module camera gặp sự cố. \\
    \bottomrule
  \end{tabularx}
\end{table}

\subsubsection{Sơ đồ trạng thái hệ thống}

Hình \cref{fig:stm32_fsm} trình bày trực quan các chuyển đổi trạng thái chính của STM32 dựa trên sự kiện (event) từ người dùng hoặc giao tiếp UART từ ESP32-S3:

\begin{figure}[H]
  \centering
  \resizebox{0.95\textwidth}{!}{%
  \begin{tikzpicture}[x=1cm, y=1cm, >=Stealth, font=\small,
    st/.style  = {draw, circle, minimum size=3.0cm, align=center,
                  fill=blue!8, draw=blue!50!black, line width=0.8pt},
    lbl/.style = {font=\scriptsize, fill=white, inner sep=2pt, align=center}
  ]
    %% ─── Layout (no text width → no hyphenation) ──────────────
    %%   Col:   x=-5.5     x=0      x=5.5
    %%   Row1:  OFFLINE    IDLE    ENROLLING   (y=1.0)
    %%   Row2:  LOCKED   UNLOCKING   DENIED    (y=-3.5)
    %%   CONNECTING at top-center  (0, 6.0)

    \node[st] (conn)     at ( 0.0,  6.0) {CONNECTING};
    \node[st] (offline)  at (-5.5,  1.0) {OFFLINE};
    \node[st] (idle)     at ( 0.0,  1.0) {IDLE};
    \node[st] (enrolling)at ( 5.5,  1.0) {ENROLLING};
    \node[st] (locked)   at (-5.5, -3.5) {LOCKED};
    \node[st] (unlocking)at ( 0.0, -3.5) {UNLOCKING};
    \node[st] (denied)   at ( 5.5, -3.5) {DENIED};

    %% CONNECTING → IDLE (straight down)
    \draw[->] (conn)     -- node[lbl, right]{READY from ESP32}   (idle);
    %% CONNECTING → OFFLINE (diagonal)
    \draw[->] (conn)     -- node[lbl, left, pos=0.4]{Timeout 30s} (offline);
    %% OFFLINE → IDLE (horizontal)
    \draw[->] (offline)  -- node[lbl, above]{STATUS resp}        (idle);

    %% IDLE ↔ ENROLLING (two parallel arcs, same row)
    \draw[->, bend left=22]  (idle)     to
      node[lbl, above]{BTN\_ENROLL / ESP ready}  (enrolling);
    \draw[->, bend left=22]  (enrolling)to
      node[lbl, below]{ENROLLED}                 (idle);

    %% ENROLLING → DENIED (straight down, same column — no crossing!)
    \draw[->] (enrolling) -- node[lbl, right]{Timeout 15s}       (denied);

    %% IDLE ↔ UNLOCKING (two parallel arcs, same column)
    \draw[->, bend left=22]  (idle)     to
      node[lbl, right]{OPEN:\textit{<id>}}        (unlocking);
    \draw[->, bend left=22]  (unlocking)to
      node[lbl, left]{Relay timeout 3s}           (idle);

    %% IDLE → DENIED (diagonal, arcs to separate from return)
    \draw[->, bend left=12]  (idle)  to
      node[lbl, above, pos=0.35]{DENIED}          (denied);
    %% DENIED → IDLE (return, bends opposite side)
    \draw[->, bend right=22] (denied)to
      node[lbl, below, pos=0.4]{2.5s auto-return} (idle);

    %% IDLE ↔ LOCKED (diagonal, two arcs)
    \draw[->, bend right=18] (idle)  to
      node[lbl, left, pos=0.4]{Failed 5$\times$}   (locked);
    \draw[->, bend right=18] (locked)to
      node[lbl, right, pos=0.6]{5 min elapsed}    (idle);

  \end{tikzpicture}}
  \caption{State Machine Diagram của STM32F411CEU6. Các trạng thái chính được điều khiển bởi sự kiện từ:
           (1) Nút nhấn người dùng (BTN\_ENROLL, BTN\_DELETE, BTN\_EXIT),
           (2) Lệnh từ ESP32-S3 qua UART (OPEN, DENIED, READY, ENROLLED, LOCKOUT),
           (3) Bộ đếm thời gian không chặn (non-blocking timers) dùng HAL\_GetTick().}
  \label{fig:stm32_fsm}
\end{figure}

\subsubsection{Mô tả chi tiết các sự kiện chuyển trạng thái}

Việc duy trì khả năng phản hồi tức thời đối với các nút nhấn đòi hỏi kiến trúc luồng chính (main loop) không bị chặn. Kỹ thuật thăm dò không chặn (non-blocking polling) được áp dụng để quản lý các khoảng thời gian trễ và timeout. 

\begin{lstlisting}[style=cstyle, caption={Xử lý đếm ngược chế độ khóa an ninh (Security Lockout)}, label={lst:lockout_code}]
case SYS_LOCKED: {
    uint32_t elapsed_ms = now - state_tick;
    /* STM32 tự động mở khóa sau thời gian lockout_duration_ms */
    if (elapsed_ms >= lockout_duration_ms) {
        lockout_last_second = 0xFFFFFFFFU;
        Restore_LinkAwareIdle(); 
    } else {
        /* Cập nhật đếm ngược lên OLED mỗi 1 giây */
        uint32_t remaining_s = (lockout_duration_ms - elapsed_ms + 999U) / 1000U;
        if (remaining_s != lockout_last_second) {
            lockout_last_second = remaining_s;
            SSD1306_ShowLockedCountdown(remaining_s);
        }
    }
    break;
}
\end{lstlisting}

Bên cạnh đó, các nút nhấn được trang bị thuật toán chống rung (debounce) phần mềm bằng cách đối chiếu dấu thời gian (\textit{timestamp}) qua hàm ngắt EXTI và đánh giá trạng thái ổn định ở vòng lặp chính.

\subsection{Bảo vệ hệ thống với Watchdog (IWDG)}

Tính ổn định của một hệ thống an ninh như khóa cửa là yếu tố tiên quyết. Firmware được bảo vệ bởi bộ giám sát phần cứng \textbf{Independent Watchdog (IWDG)} với chu kỳ reset \SI{5.0}{s}. IWDG được "nuôi" (feed) liên tục ở mỗi chu kỳ lặp. Nếu xảy ra hiện tượng treo MCU do nhiễu điện từ kích relay hoặc vòng lặp bị kẹt, IWDG sẽ tự động khởi động lại STM32, đảm bảo hệ thống luôn trong khả năng phục vụ.

\section{Firmware ESP32-S3: Tối ưu hóa Pipeline AI}
\label{sec:esp32_fw}

\subsection{Phân phối tác vụ đa nhân (Dual-Core Processing)}

Để khai thác tối đa sức mạnh của bộ xử lý Xtensa Dual-core trên ESP32-S3, firmware phân bổ các tác vụ nặng trên hai nhân độc lập nhằm duy trì luồng ảnh ổn định không bị rớt khung hình (frame drop):

\begin{itemize}
  \item \textbf{Core 0}: Đảm nhận các luồng xử lý ngoại vi như giao tiếp Camera (thu nhận pixel) và quản lý giao thức UART (\texttt{uart\_rx\_task}).
  \item \textbf{Core 1}: Dành riêng cho \texttt{fr\_task}, thực thi các thuật toán tính toán ma trận AI cường độ cao (MTMN Detect và MobileFaceNet Embed).
\end{itemize}

\subsection{Quy trình nhận diện và Cơ chế Voting}

Pipeline nhận diện khuôn mặt thực hiện tuần tự qua các bước: Phát hiện (Detection) $\to$ Cắt và canh lề (Alignment) $\to$ Trích xuất đặc trưng (Feature Extraction) $\to$ So khớp (Matching).

Để cân bằng giữa tốc độ phản hồi và độ tin cậy, hệ thống áp dụng cơ chế bỏ phiếu (\textbf{Voting Mechanism}) đơn giản: 
\begin{itemize}
  \item \textbf{Positive Voting}: Hệ thống chỉ phát lệnh \texttt{OPEN:<id>} tới STM32 khi \textbf{1 khung hình hoặc nhiều khung liên tiếp} khớp với cùng một ID (ngưỡng cosine similarity $\geq 0.45$).
  \item \textbf{Negative Voting}: Nếu \textbf{2 khung hình liên tiếp} không khớp với bất kỳ face đã đăng ký, hệ thống phát lệnh \texttt{DENIED}.
  \item \textbf{Lockout}: Sau \textbf{5 lần thất bại} liên tiếp, hệ thống kích hoạt chế độ khóa bảo vệ (\texttt{LOCKOUT}) kéo dài \SI{5}{phút}, trong đó bất kỳ lần thử nào cũng bị từ chối ngay.
\end{itemize}

Thiết kế voting tối giản này giúp phản hồi nhanh (chỉ 1 frame) nhưng có nguy cơ False Positive cao hơn. Nếu cần giảm False Accept Rate, có thể tăng \texttt{REQUIRED\_MATCHES} lên 2 hoặc 3.

\subsection{Đăng ký khuôn mặt}

Quy trình đăng ký (\textit{Enrollment}) được thiết kế tối ưu cho tốc độ và khả năng thực hiện. Khi nhận lệnh \texttt{ENROLL} từ STM32, hệ thống ESP32-S3 chuyển sang chế độ đăng ký. Hiện tại, hệ thống yêu cầu người dùng giữ tư thế \textbf{nhìn thẳng (FRONT)} kéo dài ít nhất \SI{10}{s} để phát hiện đủ stability. Một lần hoàn thành đăng ký sẽ thu suất 1 vector đặc trưng (\textit{face embedding}) lưu trữ trong bộ nhớ flash NVS của ESP32-S3.

\begin{table}[H]
  \centering
  \caption{Thông số quá trình đăng ký khuôn mặt}
  \label{tab:enroll_params}
  \begin{tabularx}{0.8\textwidth}{l X}
    \toprule
    \textbf{Tham số} & \textbf{Giá trị} \\
    \midrule
    Số tư thế yêu cầu & 1 (chỉ nhìn thẳng) \\
    Timeout phát hiện mặt/bước & \SI{10}{s} \\
    Số ID tối đa & 7 \\
    Lưu trữ & NVS Flash (on-device) \\
    \bottomrule
  \end{tabularx}
\end{table}

Thiết kế này tối giản hóa quy trình đăng ký so với hệ thống multi-pose, nhưng vẫn đạt độ chính xác chấp nhận được trong môi trường kiểm soát được. Nếu cần nâng cấp độ chính xác ở môi trường ánh sáng khó khăn, có thể tăng \texttt{ENROLL\_TOTAL\_STEPS} lên 5 và yêu cầu 5 tư thế (FRONT, LEFT, RIGHT, UP, DOWN).

\section{Giao diện hiển thị OLED SSD1306}
\label{sec:oled}

Màn hình OLED SSD1306 (128$\times$64~px) hiển thị trạng thái hoạt động theo thời gian thực. Bằng việc tận dụng bus I2C ở tốc độ \textbf{Fast-mode (400~kHz)}, giao diện luôn được cập nhật tức thì theo diễn biến của máy trạng thái.

\begin{table}[H]
  \centering
  \caption{Nội dung hiển thị OLED theo trạng thái hệ thống}
  \label{tab:oled_screens}
  \begin{tabular}{l l l}
    \toprule
    \textbf{Trạng thái} & \textbf{Nội dung chính} & \textbf{Chi tiết phụ} \\
    \midrule
    IDLE       & STM-FaceGuard  & Ready - Faces: \textit{<n>} \\
    UNLOCKED   & Xin chao!      & ID: \textit{<id>} \\
    DENIED     & Tu choi        & Khong nhan ra \\
    ENROLLING  & Dang dang ky   & Nhin thang/trai/phai... \\
    LOCKED     & KHOA AN NINH   & Cho \textit{<m>} phut \textit{<s>} giay \\
    OFFLINE    & CAMERA LOI     & Dang ket noi lai... \\
    \bottomrule
  \end{tabular}
\end{table}

\section{Hệ thống âm thanh DFPlayer Mini}
\label{sec:audio}

DFPlayer Mini là module phát âm thanh MP3 tích hợp DAC, giao tiếp qua USART6 ở tốc độ 9600~baud. Khả năng phát phản hồi bằng giọng nói song song với giao diện OLED giúp trải nghiệm người dùng trở nên trực quan và hoàn thiện.

\begin{table}[H]
  \centering
  \caption{Danh sách file âm thanh tiêu biểu}
  \label{tab:audio_tracks}
  \begin{tabularx}{\textwidth}{c X l}
    \toprule
    \textbf{Track} & \textbf{Nội dung} & \textbf{Sự kiện kích hoạt} \\
    \midrule
    1 & ``Mở cửa thành công''         & Relay mở (Lệnh \texttt{OPEN}) \\
    2 & ``Không nhận ra, vui lòng thử lại'' & Nhận diện thất bại (Lệnh \texttt{DENIED}) \\
    6-10 & Hướng dẫn quay đầu đa góc độ & Đang trong chế độ \texttt{ENROLLING} \\
    4 & ``Đăng ký khuôn mặt thành công'' & Phản hồi \texttt{ENROLLED} \\
    5 & ``Đã xóa toàn bộ dữ liệu''   & Phản hồi \texttt{DELETED} \\
    \bottomrule
  \end{tabularx}
\end{table}

\section{Phân tích bảo mật (Security Analysis)}
\label{sec:security}

Bất kỳ hệ thống nhận diện khuôn mặt nào cũng phải cân nhắc các rủi ro bảo vệ. Phần này tài liệu hóa \textit{threat model} và các cơ chế phòng chống đã triển khai trong STM-FaceGuard.

\subsection{Các mối đe dọa chính}

\subsubsection{1. Spoofing: Giả mạo khuôn mặt bằng ảnh/video}

\textbf{Mối đe dọa}: Kẻ tấn công có thể đem ảnh chân dung hoặc video quay lại của người dùng hợp lệ, đặt lên camera để hệ thống nhầm lẫn.

\textbf{Cơ chế phòng chống đã triển khai}:
\begin{itemize}
  \item \textbf{Voting 1-frame}: Bài toán nhận diện trên ảnh 2D yêu cầu sự liveness (sự sống động) tinh tế. Camera OV3660 chụp QVGA (320×240) tại 30 FPS, khiến ảnh tĩnh hoặc video lặp lại có thể phát hiện được do kém sắc nét hoặc artifact.
  \item \textbf{Enrollment verification}: Khuôn mặt được đăng ký dưới sự giám sát (enrollment on-device). Không hỗ trợ import vector từ bên ngoài.
  \item \textbf{Liveness cơ bản}: Thái độ (pose) thay đổi qua các frame liên tiếp giúp phát hiện ảnh tĩnh. Mặc dù không phải ``liveness detection'' toàn diện, nhưng sự khác biệt khuôn mặt giữa frame 1 và frame 2 có thể loại bỏ các ảnh rõ nhất.
  \item \textbf{Giới hạn}: STM-FaceGuard \textbf{không} sử dụng advanced liveness detection (challenge-response, infrared, 3D depth). Điều này là nhược điểm so với FaceID hoặc Windows Hello.
  \item \textbf{Cách khắc phục}: Có thể nâng cấp bằng cách:
    - Tích hợp Passive Liveness (PL) detection qua MSR01 model variant.
    - Yêu cầu người dùng blink/nod (active challenge), nhưng làm tăng latency.
    - Sử dụng stereo camera hoặc depth sensor để kiểm tra 3D.
\end{itemize}

\subsubsection{2. Brute-Force: Đoán ID bằng cách thử lặp lại}

\textbf{Mối đe dọa}: Kẻ tấn công có thể thử liên tiếp tới nhiều người, hoặc chụp lại ảnh của các nạn nhân có thể để chiếm quyền truy cập.

\textbf{Cơ chế phòng chống}:
\begin{itemize}
  \item \textbf{Lockout 5 lần sai}: Hệ thống chặn đoán sau 5 lần từ chối liên tiếp, khóa trong 5 phút (300,000 ms). Quota này được lưu trong RAM STM32 và reset sau 5 phút.
  \item \textbf{Temporal Rate Limiting}: Thay vì chặn ngay từ lần 3, hệ thống cho phép đến 5 lần sai trước khi khóa - đủ để người dùng hợp lệ retry trong lần 2 (do góc lệch hoặc ánh sáng), nhưng đủ hạn chế để ngăn chặn đoán dự đoán hàng loạt.
  \item \textbf{Lockout cứng}: Trong 5 phút khóa, OLED hiển thị bộ đếm ngược để người dùng hợp lệ biết rằng cần chờ. Khóa được mở tự động (non-invasive) sau 5 phút.
  \item \textbf{Giới hạn}: Nếu ai đó có thể chạy 5 lần sai mỗi 5 phút, họ có thể thử 288 lần/ngày. Tuy không phải bảo vệ mạnh nhất, nhưng đủ cho lock khóa thông minh hộ gia đình.
  \item \textbf{Cách khắc phục}: 
    - Tích hợp logging bảo vệ: Ghi nhân các lần sai không thành công (hoặc gửi cảnh báo qua WiFi).
    - Exponential backoff: Tăng thời gian khóa từ 5 phút → 15 phút → 1 giờ cho các lần khóa liên tiếp.
\end{itemize}

\subsubsection{3. Side-Channel Attack: Phân tích độ trễ hoặc công suất}

\textbf{Mối đe dọa}: Kẻ tấn công lắng nghe độ trễ phản hồi hoặc sóng điện từ để suy ra thông tin về khuôn mặt được đăng ký hoặc trạng thái nội bộ (timing attack).

\textbf{Cơ chế phòng chống}:
\begin{itemize}
  \item \textbf{Constant-time matching}: Hàm so khớp cosine similarity được thiết kế truy cập mảng embedding theo cư dụng cố định, không có early exit. Dù khuôn mặt khớp hay không, độ trễ tính toán gần như nhau ($\sim 5$--10 ms).
  \item \textbf{Không lộ lý lịch}: Kẻ tấn công không thể biết được số lượng ID trong cơ sở dữ liệu từ độ trễ phản hồi.
  \item \textbf{Giới hạn}: Không có bảo vệ electromagnetic (EMI shielding) cụ thể. Nếu cần bảo vệ chống lạm dụng công suất, cần phải thêm bộ lọc hoặc mã hóa lưu lượng.
\end{itemize}

\subsubsection{4. Physical Attack (Tấn công vật lý): Phá hủy camera hoặc relay}

\textbf{Mối đe dọa}: Kẻ tấn công có thể phá hủy vật lý camera hoặc module điều khiển relay để làm mất chức năng hệ thống.

\textbf{Cơ chế phòng chống}:
\begin{itemize}
  \item \textbf{Dual-circuit design}: Relay có 2 ngõ ra (NO + NC). Có thể thêm cảm biến cơ học để phát hiện mở khóa ngoại lệ (unauthorized unlock).
  \item \textbf{Watchdog}: Nếu camera bị hỏng (không có ảnh), ESP32 sẽ không gửi lệnh OPEN, STM32 sẽ timeout và chuyển sang OFFLINE.
  \item \textbf{Giới hạn}: Không có phòng chống vật lý cao cấp (không có tamper detection, không có nước mắt mạch, không có sealed enclosure bí mật).
  \item \textbf{Cách khắc phục}: Đặt hệ thống trong enclosure chống xâm nhập (IP65+), thêm cảm biến vibration/shock để phát hiện bất thường vật lý.
\end{itemize}

\subsection{So sánh với các hệ thống khác}

\begin{table}[H]
  \centering
  \caption{So sánh các cơ chế bảo mật giữa STM-FaceGuard và các hệ thống tương tự}
  \label{tab:security_comparison}
  \begin{tabularx}{0.95\textwidth}{l c c c X}
    \toprule
    \textbf{Cơ chế bảo mật} & \textbf{STM-FaceGuard} & \textbf{Windows Hello} & \textbf{Apple FaceID} & \textbf{Khóa cửa thông minh} \\
    \midrule
    Liveness detection & Bị động (1-frame) & Chủ động (IR patterns) & 3D depth-based & Bị động hoặc không \\
    Anti-spoofing & Đơn giản & Cao (TrueDepth) & Rất cao & Trung bình \\
    Rate limiting & 5 lần/5 phút & Không giới hạn & Exp.\ backoff & 5--10 lần/30 phút \\
    Lockout duration & 5 phút & Cơ chế ít rõ & 5 phút tăng dần & 30 phút - vĩnh viễn \\
    Encryption level & Không & TPM + Win Vault & Secure Enclave & Thường không \\
    Logging/Reporting & Không & \checkmark~Win Log & \checkmark~iCloud & Tùy thiết bị \\
    \bottomrule
  \end{tabularx}
\end{table}

\subsection{Định vị rủi ro bảo mật}

Nhân danh tính chất của dự án sinh viên và hạn chế về thời gian/tài nguyên, STM-FaceGuard được thiết kế cho \textbf{bảo vệ cơ bản mức hộ gia đình}, không phải doanh nghiệp hoặc ngân hàng. Các rủi ro chính:

\begin{enumerate}
  \item \textbf{Spoofing bằng ảnh/video} được giảm nhẹ nhưng không loại bỏ hoàn toàn.
  \item \textbf{Brute-force} bị giới hạn bởi rate limiting nhưng không toàn bộ bảo vệ lưu lượng cao.
  \item \textbf{Side-channel} không phải mục tiêu thiết kế, mặc dù có constant-time matching.
  \item \textbf{Vật lý} không có bảo vệ, yêu cầu mechanical locks bổ sung.
\end{enumerate}

Nếu cần dùng hệ thống cho mục đích quan trọng hơn, cần phải tiến hành audit bảo mật thuyên chuyên (penetration testing) và cập nhật firmware với các mô-đun liveness detection cao cấp.
