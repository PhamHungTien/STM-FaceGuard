% ============================================================
%  CHƯƠNG 2: CƠ SỞ LÝ THUYẾT
% ============================================================
\chapter{Cơ sở lý thuyết}
\label{chap:theory}

\section{Nhận diện khuôn mặt}
\label{sec:face_recognition}

\subsection{Tổng quan}

Nhận diện khuôn mặt (\textit{Face Recognition --- FR}) là bài toán xác định hoặc xác nhận danh tính của một cá nhân dựa trên đặc trưng hình ảnh khuôn mặt. Hệ thống FR điển hình gồm ba giai đoạn nối tiếp \cite{wang2021}:

\begin{enumerate}[label=\arabic*.]
  \item \textbf{Phát hiện khuôn mặt} (\textit{Face Detection}): Xác định vị trí và kích thước vùng khuôn mặt trong ảnh thô từ camera. Đây là bước tiền xử lý quan trọng nhất vì mọi giai đoạn tiếp theo đều phụ thuộc vào chất lượng phát hiện.
  \item \textbf{Trích xuất đặc trưng} (\textit{Feature Extraction}): Cắt vùng khuôn mặt, chuẩn hóa kích thước và đưa qua mô hình CNN để sinh ra vector đặc trưng (\textit{face embedding}) $\mathbf{f} \in \mathbb{R}^d$ mang tính đặc trưng của từng cá nhân. Khoảng cách giữa hai embedding phản ánh mức độ tương đồng giữa hai khuôn mặt.
  \item \textbf{So khớp và nhận dạng} (\textit{Matching}): So sánh embedding đầu vào với cơ sở dữ liệu bằng độ đo cosine similarity hoặc khoảng cách Euclidean. Nếu khoảng cách nhỏ hơn ngưỡng $\theta$, hệ thống chấp nhận danh tính; ngược lại từ chối.
\end{enumerate}

Độ chính xác của FR được đánh giá qua hai chỉ số chính: \textit{False Acceptance Rate} (FAR) --- tỉ lệ người lạ được chấp nhận nhầm; và \textit{False Rejection Rate} (FRR) --- tỉ lệ người được đăng ký bị từ chối nhầm.

\subsection{Mô hình sử dụng trên ESP32-S3: MobileFaceNet}

Do tài nguyên tính toán trên MCU bị giới hạn, hệ thống sử dụng \textbf{MobileFaceNet} \cite{chen2018} --- một kiến trúc mạng nơ-ron nhỏ gọn được thiết kế đặc biệt cho thiết bị di động và nhúng. MobileFaceNet sử dụng các khối \textit{depthwise separable convolution} để giảm số phép nhân ma trận xuống đáng kể so với CNN thông thường, trong khi vẫn đạt độ chính xác cao trên tập benchmark LFW (Labeled Faces in the Wild).

Mô hình được chạy qua framework \textbf{ESP-WHO} của Espressif, đã được tối ưu hóa bằng phép lượng tử hóa số nguyên 8-bit (INT8 quantization) để phù hợp với kiến trúc vector AI tích hợp trong ESP32-S3. Quy trình phát hiện khuôn mặt sử dụng kiến trúc hai giai đoạn (Two-Stage Detection): 
\begin{itemize}
  \item \textbf{Giai đoạn 1 (MSR01)}: Phát hiện sơ bộ vùng khuôn mặt với threshold = 0.15, NMS = 0.5, tối đa 10 vùng.
  \item \textbf{Giai đoạn 2 (MNP01)}: Tinh chỉnh vị trí keypoints với threshold = 0.5, NMS = 0.3, tối đa 5 vùng.
\end{itemize}
Ngưỡng tương đồng cosine được đặt ở $\theta = 0.45$ để cân bằng giữa chặn lại nhân diện sai (FAR) và từ chối nhân diện đủng (FRR) trong môi trường ánh sáng trung bình.

\section{Kiến trúc hệ thống nhúng đa vi điều khiển}
\label{sec:dual_mcu}

\subsection{Động cơ thiết kế Dual-MCU}

Phương án dùng một MCU duy nhất cho cả xử lý AI lẫn điều khiển ngoại vi dẫn đến hai vấn đề: (1) ESP32-S3 không có đủ ngoại vi phù hợp (ví dụ: thiếu analog comparator, timer PWM chuyên dụng cho âm thanh 9600 baud theo giao thức DFPlayer), và (2) tải xử lý nặng từ thuật toán FR có thể gây trễ cho các tác vụ điều khiển real-time.

Kiến trúc Dual-MCU giải quyết điều này bằng cách phân tách nhiệm vụ theo nguyên tắc \textit{separation of concerns}:

\begin{itemize}
  \item \textbf{ESP32-S3-N16R8} --- \textit{AI Processor}: Xử lý camera, chạy mô hình FR, quản lý cơ sở dữ liệu khuôn mặt trong bộ nhớ flash 16~MB. Phần cứng bao gồm lõi Xtensa LX7 kép 240~MHz và đơn vị tăng tốc vector (PIE\allowbreak\ ---\ Processor Instruction Extensions).
  \item \textbf{STM32F411CEU6} --- \textit{Peripheral Controller}: Quản lý toàn bộ ngoại vi, xử lý ngắt nút nhấn, điều khiển relay/LED/OLED/âm thanh và thực thi máy trạng thái hệ thống. Lõi ARM Cortex-M4F 100~MHz với FPU, sử dụng thư viện HAL trong STM32CubeIDE.
\end{itemize}

\subsection{Giao thức UART nội bộ}

Hai vi điều khiển giao tiếp qua UART ở tốc độ \SI{115200}{baud} với giao thức văn bản ASCII đơn giản, mỗi lệnh kết thúc bằng ký tự xuống dòng \texttt{\textbackslash n} (0x0A). Thiết kế văn bản giúp dễ debug qua terminal và không cần thư viện phân tích giao thức phức tạp.

\begin{table}[H]
  \centering
  \caption{Giao thức UART giữa ESP32-S3 và STM32F411CEU6 và các thông số nhận diện}
  \label{tab:uart_protocol}
  \begin{tabularx}{0.95\textwidth}{>{\ttfamily}l c X}
    \toprule
    \textbf{Lệnh / Tham số} & \textbf{Chiều} & \textbf{Ý nghĩa} \\
    \midrule
    \multicolumn{3}{l}{\textbf{Lệnh từ ESP32 tới STM32}} \\
    OPEN:\textit{<id>}\textbackslash n     & $\to$ STM32 & Nhận diện thành công ID \textit{<id>}, yêu cầu mở khóa \\
    DENIED\textbackslash n                 & $\to$ STM32 & Không nhận ra khuôn mặt, từ chối truy cập \\
    ENROLLED:\textit{<id>}\textbackslash n & $\to$ STM32 & Đăng ký khuôn mặt thành công, ID = \textit{<id>} \\
    DELETED\textbackslash n                & $\to$ STM32 & Xóa toàn bộ dữ liệu thành công \\
    READY\textbackslash n                  & $\to$ STM32 & Khởi động xong, sẵn sàng hoạt động \\
    LOCKOUT\textbackslash n                & $\to$ STM32 & Khóa an ninh: quá nhiều lần nhận diện thất bại \\
    \midrule
    \multicolumn{3}{l}{\textbf{Lệnh từ STM32 tới ESP32}} \\
    ENROLL\textbackslash n                 & $\leftarrow$ STM32 & Bắt đầu chế độ đăng ký khuôn mặt (1 tư thế) \\
    DEL\_ALL\textbackslash n               & $\leftarrow$ STM32 & Xóa toàn bộ cơ sở dữ liệu khuôn mặt \\
    STATUS\textbackslash n                 & $\leftarrow$ STM32 & Yêu cầu ESP32 gửi lại READY/FACES để đồng bộ \\
    \midrule
    \multicolumn{3}{l}{\textbf{Thông số nhận diện chính}} \\
    Ngưỡng cosine similarity              &  - & $\theta = 0.45$ \\
    Voting (frame khớp liên tiếp)         &  - & $N = 1$ frame (mở cửa ngay lập tức) \\
    Timeout phát hiện mặt (enrollment)    &  - & \SI{10}{s}/bước \\
    Max số face đăng ký                   &  - & 7 khuôn mặt \\
    Lockout số lần sai                    &  - & 5 lần thất bại $\to$ khóa 5 phút \\
    \bottomrule
  \end{tabularx}
\end{table}

\section{So sánh với các giải pháp hiện có}
\label{sec:related_work}

Mục này so sánh kiến trúc và hiệu năng của dự án STM-FaceGuard với các giải pháp smart lock khác đã được công bố:

\begin{table}[H]
  \centering
  \caption{So sánh dự án STM-FaceGuard với các giải pháp nhận diện khuôn mặt khác}
  \label{tab:comparison_existing}
  \begin{tabularx}{0.95\textwidth}{l c c c c}
    \toprule
    \textbf{Tiêu chí} & \textbf{STM-FaceGuard} & \textbf{Daon Face Lock} & \textbf{DoorBell AI} & \textbf{Cloud-based} \\
    \midrule
    Xử lý local & ✓ & ✓ & Hybrid & ✗ \\
    Không internet & ✓ & ✓ & ✗ & ✗ \\
    Dual-MCU & ✓ & ✗ & ✓ & N/A \\
    Giá (~USD) & 50--80 & 120--150 & 30--50 & 10--20/tháng \\
    Độ chính xác (\%) & 90--95 & 95--98 & 80--85 & 99+ \\
    Latency (s) & 0.13--0.5 & 0.5--1.5 & 0.2--0.8 & 2--5 \\
    Độc lập dữ liệu & ✓ & ✓ & Hybrid & ✗ \\
    \bottomrule
  \end{tabularx}
\end{table}

\noindent\textbf{Nhận xét:}
\begin{itemize}
  \item STM-FaceGuard chọn mô hình **local-first, DIY-friendly** với độ chính xác chấp nhận được (90--95\%), phù hợp cho ứng dụng dân dụng như khóa nhà.
  \item So với Daon Face Lock hay các giải pháp commercial, chúng tôi tối ưu **cost-effectiveness** thay vì **highest accuracy**.
  \item So với cloud-based solutions, lợi thế chính là **privacy** (dữ liệu khuôn mặt không bao giờ lên server) và **latency thấp** (không phụ thuộc internet).
  \item Kiến trúc Dual-MCU của chúng tôi tuân theo best practice trong embedded systems: tách biệt **compute-intensive AI** (ESP32) khỏi **real-time control** (STM32).
\end{itemize}

\section{Các chuẩn giao tiếp sử dụng}
\label{sec:protocols}

\subsection{UART (Universal Asynchronous Receiver-Transmitter)}

UART là chuẩn giao tiếp nối tiếp không đồng bộ điểm-điểm (point-to-point). Mỗi khung dữ liệu (\textit{data frame}) gồm: 1 bit start (mức thấp), 8 bit dữ liệu (LSB trước), không có bit parity, và 1 bit stop (mức cao). Tốc độ baud xác định thời gian mỗi bit: ở \SI{115200}{baud}, mỗi bit kéo dài \SI{8.68}{\micro\second}.

Hệ thống sử dụng ba kênh UART trên STM32:
\begin{itemize}
  \item \textbf{USART1} (\SI{115200}{baud}): Giao tiếp với ESP32-S3 --- kênh dữ liệu chính.
  \item \textbf{USART2} (\SI{38400}{baud}): Giao tiếp với ST-Link Virtual COM --- dùng để debug.
  \item \textbf{USART6} (\SI{9600}{baud}): Điều khiển module DFPlayer Mini --- phát âm thanh.
\end{itemize}

\subsection{I2C (Inter-Integrated Circuit)}

I2C là chuẩn giao tiếp nối tiếp đồng bộ đa điểm (multi-master, multi-slave) chỉ cần 2 dây: SDA (dữ liệu) và SCL (xung nhịp). Mỗi thiết bị slave có địa chỉ 7-bit duy nhất; master khởi tạo giao dịch bằng điều kiện START và kết thúc bằng STOP. Mỗi byte được xác nhận bằng bit ACK/NACK từ phía nhận.

Hệ thống sử dụng I2C1 ở chế độ Standard-mode (\SI{100}{kHz}) để giao tiếp với màn hình OLED SSD1306 (địa chỉ 0x3C). Tốc độ này đủ để cập nhật giao diện OLED mà người dùng không cảm nhận độ trễ, đồng thời đảm bảo độ ổn định tín hiệu trên đường dây thực tế.
