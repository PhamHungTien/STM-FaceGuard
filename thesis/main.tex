% ============================================================
%  TIỂU LUẬN: Thiết Kế Khóa Thông Minh Nhận Diện Khuôn Mặt
%  Engine: XeLaTeX  |  Bibliography: Biber
%  Build:  make  (hoặc xelatex → biber → xelatex × 2)
% ============================================================
\documentclass[12pt, a4paper, twoside, openright]{report}

% ── Encoding & Font ─────────────────────────────────────────
\usepackage{fontspec}
\usepackage{polyglossia}
\setmainlanguage{vietnamese}
\setotherlanguage{english}
\setmainfont{Times New Roman}
\setsansfont{Arial}
\setmonofont[Scale=0.88]{Courier New}

% ── Page Layout ─────────────────────────────────────────────
\usepackage[
  a4paper,
  top=2.5cm, bottom=2.5cm,
  left=3.0cm, right=2.0cm,
  headheight=15pt
]{geometry}

% ── Line Spacing ────────────────────────────────────────────
\usepackage{setspace}
\usepackage{scrextend}
\changefontsizes{13pt}
\onehalfspacing

% ── Headers & Footers ───────────────────────────────────────
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[LE]{\small\itshape\leftmark}
\fancyhead[RO]{\small\itshape\rightmark}
\fancyfoot[C]{\small\thepage}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\footrulewidth}{0pt}
% Định dạng mark gọn hơn cho header
\renewcommand{\chaptermark}[1]{\markboth{Chương \thechapter.\ #1}{}}
\renewcommand{\sectionmark}[1]{\markright{\thesection\ #1}}
% Trang đầu chương (plain style): chỉ số trang, không header
\fancypagestyle{plain}{%
  \fancyhf{}
  \fancyfoot[C]{\small\thepage}
  \renewcommand{\headrulewidth}{0pt}
}

% ── Section Title Formatting ────────────────────────────────
\usepackage{titlesec}
\titleformat{\chapter}[block]
  {\normalfont\Large\bfseries\centering}
  {CHƯƠNG \thechapter.}{0.6em}{\MakeUppercase}
\titlespacing*{\chapter}{0pt}{0pt}{30pt}

\titleformat{\section}
  {\normalfont\large\bfseries}
  {\thesection}{1em}{}
\titlespacing*{\section}{0pt}{12pt}{6pt}

\titleformat{\subsection}
  {\normalfont\normalsize\bfseries}
  {\thesubsection}{1em}{}
\titlespacing*{\subsection}{0pt}{10pt}{4pt}

\titleformat{\subsubsection}
  {\normalfont\normalsize\itshape\bfseries}
  {\thesubsubsection}{1em}{}

% ── Counter: đánh số hình, bảng, mã nguồn theo chương ──────
\usepackage{chngcntr}
\counterwithin{figure}{chapter}
\counterwithin{table}{chapter}

% ── Table of Contents Styling ────────────────────────────────
\usepackage{tocloft}
\renewcommand{\cftchapfont}{\bfseries}
\renewcommand{\cftchappagefont}{\bfseries}
\renewcommand{\cftchapleader}{\bfseries\cftdotfill{\cftdotsep}}
\setlength{\cftbeforechapskip}{6pt}
\setlength{\cftbeforesecskip}{2pt}

% ── Graphics & Figures ──────────────────────────────────────
\usepackage{graphicx}
\graphicspath{{figures/}}
\usepackage{float}
\usepackage{caption}
\usepackage{subcaption}
\captionsetup{
  labelfont=bf,
  labelsep=period,
  font=small,
  width=0.92\textwidth
}
\captionsetup[figure]{name=Hình}
\captionsetup[table]{name=Bảng}
\usepackage{pdflscape}              % trang ngang cho bảng lớn

% ── Tables ──────────────────────────────────────────────────
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{multirow}
\usepackage{longtable}
\usepackage{array}
\usepackage{colortbl}
\usepackage{xcolor}
\newcolumntype{Y}{>{\raggedright\arraybackslash}X}
\definecolor{tableheader}{RGB}{220,230,242}
\definecolor{codeframe}{RGB}{200,200,200}
\definecolor{codebg}{RGB}{248,248,248}
\definecolor{kwcolor}{RGB}{0,0,200}
\definecolor{cmtcolor}{RGB}{100,100,100}
\definecolor{strcolor}{RGB}{180,60,0}
\definecolor{numcolor}{RGB}{150,0,120}

% ── Source Code Listing ─────────────────────────────────────
\usepackage{listings}
\renewcommand{\lstlistingname}{Mã nguồn}
\renewcommand{\lstlistlistingname}{Danh mục mã nguồn}
\lstset{
  basicstyle=\ttfamily\small\setstretch{1.0},
  keywordstyle=\color{kwcolor}\bfseries,
  commentstyle=\color{cmtcolor},
  stringstyle=\color{strcolor},
  numberstyle=\tiny\color{gray},
  numbers=left,
  stepnumber=1,
  numbersep=10pt,
  backgroundcolor=\color{codebg},
  frame=single,
  framerule=0.4pt,
  rulecolor=\color{codeframe},
  breaklines=true,
  breakatwhitespace=true,
  showstringspaces=false,
  keepspaces=true,
  columns=fullflexible,
  upquote=true,
  tabsize=4,
  captionpos=b,
  xleftmargin=1.5em,
  xrightmargin=0.5em,
  aboveskip=8pt,
  belowskip=8pt
}
\lstdefinestyle{cstyle}{
  language=C,
  morekeywords={uint8_t,uint16_t,uint32_t,int8_t,int16_t,int32_t,
                GPIO_PinState,HAL_StatusTypeDef,UART_HandleTypeDef,
                volatile}
}
\lstdefinestyle{pystyle}{language=Python}
% Đánh số mã nguồn theo chương (listings định nghĩa counter khi loaded)
\AtBeginDocument{\counterwithin{lstlisting}{chapter}}

% ── Math ────────────────────────────────────────────────────
\usepackage{amsmath, amssymb, amsthm}

% ── SI Units ────────────────────────────────────────────────
\usepackage{siunitx}
\sisetup{
  output-decimal-marker = {,},
  group-separator       = {.}
}

% ── Diagrams ────────────────────────────────────────────────
\usepackage{tikz}
\usetikzlibrary{shapes.geometric, shapes.misc, arrows.meta, positioning, calc, fit}
\usepackage{pgfgantt}

% ── Hyperlinks (phải load sau cùng, trước cleveref) ─────────
\usepackage{csquotes}               % bắt buộc với biblatex + polyglossia
\DeclareQuoteAlias{german}{vietnamese}  % fallback quote style cho tiếng Việt
\usepackage[
  colorlinks  = true,
  linkcolor   = black,
  citecolor   = blue!65!black,
  urlcolor    = blue!65!black,
  bookmarks   = true,
  bookmarksnumbered = true
]{hyperref}
% Metadata UTF-8 khai báo riêng để tránh lỗi keyval parsing
\hypersetup{
  pdftitle    = {Thiet Ke Khoa Thong Minh Nhan Dien Khuon Mat},
  pdfauthor   = {Pham Hung Tien, Tran Thai Nguyen, Ho Si Phu,
                 Quach Gia Thinh, Le Quang Bao Trung},
  pdfsubject  = {He thong nhung - Nhan dien khuon mat},
  pdfkeywords = {STM32, ESP32-S3, Face Recognition, Smart Lock, Embedded}
}

% ── Smart cross-references ──────────────────────────────────
% cleveref + polyglossia Vietnamese không tương thích natively;
% fix: force cleveref dùng English hooks, sau đó override tên thủ công.
\makeatletter
\def\cref@language{english}
\makeatother
\usepackage{cleveref}
\crefname{figure}{Hình}{Hình}
\crefname{table}{Bảng}{Bảng}
\crefname{listing}{Mã nguồn}{Mã nguồn}
\crefname{chapter}{Chương}{Chương}
\crefname{section}{Mục}{Mục}
\crefname{equation}{Công thức}{Công thức}

% ── Bibliography ────────────────────────────────────────────
\usepackage[
  backend  = biber,
  style    = ieee,
  sorting  = none,
  maxnames = 5,
  language = english,
  autolang = other
]{biblatex}
\DeclareLanguageMapping{vietnamese}{english}
\addbibresource{references.bib}

% ── Miscellaneous ───────────────────────────────────────────
\usepackage{enumitem}
\setlist[itemize]{topsep=4pt, itemsep=2pt, parsep=0pt}
\setlist[enumerate]{topsep=4pt, itemsep=2pt, parsep=0pt}
\usepackage{microtype}
\raggedbottom                       % tránh Underfull vbox
\emergencystretch=2em               % tránh Overfull hbox do từ dài
\hyphenation{MobileFaceNet}
\usepackage{emptypage}              % trang trắng không có header/footer

% ============================================================
%  DOCUMENT
% ============================================================
\begin{document}

\pagenumbering{gobble}

% ── Trang bìa ───────────────────────────────────────────────
\input{chapters/00_cover}

\cleardoublepage
\pagenumbering{roman}

% ── Tóm tắt ─────────────────────────────────────────────────
\input{chapters/00_abstract}
\cleardoublepage

% ── Mục lục ─────────────────────────────────────────────────
\markboth{Mục lục}{Mục lục}
\tableofcontents
\cleardoublepage

% ── Danh mục hình vẽ ────────────────────────────────────────
\phantomsection
\addcontentsline{toc}{chapter}{Danh mục hình vẽ}
\markboth{Danh mục hình vẽ}{Danh mục hình vẽ}
\listoffigures
\cleardoublepage

% ── Danh mục bảng biểu ──────────────────────────────────────
\phantomsection
\addcontentsline{toc}{chapter}{Danh mục bảng biểu}
\markboth{Danh mục bảng biểu}{Danh mục bảng biểu}
\listoftables
\cleardoublepage

% ── Danh mục từ viết tắt ────────────────────────────────────
\input{chapters/00_abbreviations}
\cleardoublepage

\pagenumbering{arabic}

% ── Các chương nội dung ─────────────────────────────────────
\input{chapters/ch1_intro}
\input{chapters/ch2_theory}
\input{chapters/ch3_hardware}
\input{chapters/ch4_software}
\input{chapters/ch5_results}
\input{chapters/ch6_conclusion}

% ── Tài liệu tham khảo ──────────────────────────────────────
\cleardoublepage
\phantomsection
\addcontentsline{toc}{chapter}{Tài liệu tham khảo}
\markboth{Tài liệu tham khảo}{Tài liệu tham khảo}
\printbibliography[title={Tài liệu tham khảo}]

% ── Phụ lục ─────────────────────────────────────────────────
\appendix
\input{appendix/schematic}
\input{appendix/code_snippets}

% ── Phân công công việc (tài liệu hành chính) ───────────────
\cleardoublepage
\input{chapters/00_task_division}

\end{document}
